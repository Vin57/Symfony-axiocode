{% extends 'base.html.twig' %}

{% block title %}{{ "Edit"|trans }} "{{ product.name|susbtr_dotpad(20) }}"{% endblock %}

{% block body %}
    <h1>Edit Product</h1>

    {{ include('product/form/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('product_index') }}">back to list</a>

    {{ include('product/form/_delete_form.html.twig') }}
{% endblock %}
{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('form_collection') }}
{% endblock %}